# Contract Testing with Spring Cloud Contract

Source code for demo presented at KotlinEverywhere in Lima

1. Run `producer` project using `./mvnw clean install`
2. Run `consumer` project using `./mvnw test`
